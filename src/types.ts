export type CardProps = {
    image: any;
    title: string;
    description: string;
};

export type TitleProps = {
    title: string;
    description: string;
}

export interface DriveFile {
  id: string;
  name: string;
  mimeType: string;
}
